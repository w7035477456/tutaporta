/**
 * Soft UX warning when rate-limit usage reaches RATE_LIMIT_WARN_PERCENT.
 * Periodic polling disabled — no background GET /api/rateLimitStatus every 2s.
 */
export default function RateWarnPopup() {
  return null;
}
