/** Canonical Record Vault URL (/myRecordVault redirects here). */
export const MY_RECORD_VAULT_PATH = '/myNote';

/** Legacy path — kept for route checks during redirect. */
export const MY_RECORD_VAULT_LEGACY_PATH = '/myRecordVault';
