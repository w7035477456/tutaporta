export default {
  async fetch(request, env, ctx) {
    try {
      const response = await fetch(request);
      const pathname = new URL(request.url).pathname;
      const isApiRequest = pathname.startsWith("/api/");
      // Keep JSON API error bodies for the SPA; only replace HTML page navigations.
      if (response.status >= 500 && !isApiRequest) {
        return new Response(customHtml, {
          status: response.status,
          headers: { "content-type": "text/html;charset=UTF-8" },
        });
      }
      return response;
    } catch (err) {
      // If origin is completely down, show custom page
      return new Response(customHtml, {
        status: 503,
        headers: { "content-type": "text/html;charset=UTF-8" },
      });
    }
  },
};

const customHtml = `
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service Update | OnlineMall</title>
    <style>
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            text-align: center; 
            padding: 80px 20px; 
            background-color: var(--theme-primary-color);
            color: var(--theme-primary-color);
        }
        .container { max-width: 600px; margin: 0 auto; }
        
        /* New Style for your Maintenance Graphic */
        .maintenance-graphic {
            max-width: 100%;
            height: auto;
            margin-bottom: 30px;
        }
        
        h1 { color: var(--theme-primary-color); font-size: 2.5em; margin-top: 0; }
        p { line-height: 1.6; font-size: 1.1em; color: var(--theme-primary-color); }
        .support-box { 
            margin-top: 30px; 
            padding: 20px; 
            background: var(--theme-primary-color); 
            border-radius: 8px; 
            box-shadow: 0 4px 6px rgba(0,0,0,0.1); 
        }
        a { color: var(--theme-primary-color); text-decoration: none; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <img src="https://images.unsplash.com/photo-1584824486509-112e4181ff6b?q=80&w=600&auto=format&fit=crop" 
     alt="Maintenance" 
     class="maintenance-graphic" 
     style="width: 100%; max-width: 400px; border-radius: 8px;">

<h1 style="color: var(--theme-primary-color);">We're Fine-Tuning Things</h1>
        <h2>https://tutamall.com</h1>
        <p style="color: var(--theme-error-color);">We are currently performing some essential updates to improve your shopping experience at OnlineMall. We know this is inconvenient, and we sincerely apologize for the interruption.</p>
        <p>Our team is working hard to get everything back to normal as quickly as possible. We truly appreciate your patience and your support.</p>
        
        <div class="support-box">
            <p>Have urgent questions regarding your orders or account?</p>
            <p>Please reach out to us at: <br> 
            <strong><a href="mailto:support@tutamall.com">support@tutamall.com</a></strong></p>
        </div>
        <p><small>Thank you for being part of the OnlineMall.Website community.</small></p>
    </div>
</body>
</html>
`;